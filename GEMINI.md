# Gemini Adapter for Hermes Relay

Read and follow `AGENTS.md`; it is the canonical repository contract. Then read the relevant source-owned document under `docs/`, plus `RELEASE.md` for release work or `TODO.md` for deferred work.

Do not duplicate endpoint tables, current versions, private infrastructure, or release state here. Verify changing upstream Hermes behavior from current upstream source and run the validation commands for the surface changed.
